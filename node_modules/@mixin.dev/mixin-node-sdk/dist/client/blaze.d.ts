import WebSocket from 'ws';
import Keystore from './types/keystore';
import { BlazeOptions, BlazeHandler } from './types';
export declare const BlazeKeystoreClient: (keystore: Keystore | undefined, wsOptions: BlazeOptions | undefined) => {
    loop: (h: BlazeHandler) => void;
    stopLoop: () => void;
    getWebSocket: () => WebSocket | undefined;
};
export declare const BlazeClient: (keystore: Keystore, wsOptions?: BlazeOptions | undefined) => {
    blaze: {
        loop: (h: BlazeHandler) => void;
        stopLoop: () => void;
        getWebSocket: () => WebSocket | undefined;
    };
};
export default BlazeClient;
